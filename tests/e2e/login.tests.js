/*describe('Testing login screen ', function(){  
    var loginButton;
    
    beforeEach(function() {
        browser.ignoreSynchronization = true;
        loginButton = element(by.id('login'));
    });

    it('Press the login button, and expect to find the correct user', function() {
        // TODO: test successful logic
        loginButton.click();
        var displayName = element(by.id('displayName'));
        expect(displayName.getText()).toContain('Subhag Oak');
    })
});*/